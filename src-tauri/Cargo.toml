[package]
name = "devdy"
version = "0.5.0"
description = "AI coding agent for developers"
authors = ["thainph"]
edition = "2021"

[lib]
name = "devdy_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["protocol-asset"] }
tauri-plugin-opener = "2"
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
tauri-plugin-shell = "2"
tauri-plugin-notification = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
tokio = { version = "1", features = ["full"] }
sqlx = { version = "0.8", features = ["sqlite", "runtime-tokio", "migrate", "macros"] }
keyring = { version = "3", features = ["apple-native", "windows-native", "sync-secret-service"] }
anyhow = "1"
async-trait = "0.1"
thiserror = "2"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
sha2 = "0.10"
hex = "0.4"
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1", features = ["v4"] }
notify = "6"
notify-rust = "4"
zip = "2"
walkdir = "2"
ignore = "0.4"
# Move files to the OS trash (recoverable delete) for the file-tree context menu.
trash = "5"
octocrab = { version = "0.44", default-features = false, features = ["default-client", "rustls", "rustls-ring", "rustls-webpki-tokio"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
# Remote Control (C1): outbound WSS client to the relay + shared E2E crypto.
tokio-tungstenite = { version = "0.24", features = ["rustls-tls-webpki-roots"] }
futures-util = { version = "0.3", default-features = false, features = ["sink", "std"] }
rand = "0.8"
base64 = "0.22"
remote-e2e = { path = "../remote-e2e" }

[target.'cfg(unix)'.dependencies]
# Process-group SIGKILL on app exit / cancel, so sidecar + CLI children don't
# linger and burn API tokens after the app closes.
libc = "0.2"
