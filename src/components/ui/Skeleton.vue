<script setup lang="ts">
// A single shimmering placeholder block. Compose several to mirror the real
// layout being loaded (icon + text lines + badges) so the wait reads as
// "content is coming" rather than a blank/pulsing box. Pass sizing/rounding via
// class, e.g. <Skeleton class="h-4 w-32 rounded" />. Defaults to rounded-md.
</script>

<template>
  <div class="skeleton rounded-md" />
</template>
