#!/usr/bin/env node
// Devdy git credential helper entry — see git-credential-devdy.mjs. git invokes
// this as `git-credential-devdy <action>` (action ∈ get/store/erase) when it is
// configured as credential.helper via GIT_CONFIG_* for the run.
import { run } from './git-credential-devdy.mjs'

run(process.argv[2]).finally(() => process.exit(0))
